import { MobileStatistics } from './mobileStatistics.model';

describe('MobileStatistics', () => {
  it('should create an instance', () => {
    expect(new MobileStatistics()).toBeTruthy();
  });
});
